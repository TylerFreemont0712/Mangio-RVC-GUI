# =============================================================================
# Mangio-RVC-Fork — Python dependencies
#
# Direct dependencies only.  Transitive deps (pulled automatically by pip)
# are intentionally omitted to keep the file maintainable.
#
# GPU note: install the CUDA-specific torch/torchaudio builds from
#   https://pytorch.org/get-started/locally/ *before* running pip install -r.
# =============================================================================

# ── Core ML framework ────────────────────────────────────────────────────────
torch>=2.0.0
torchaudio>=2.0.0
torchcrepe>=0.0.20
fairseq>=0.12.2

# ── Scientific / numerical ───────────────────────────────────────────────────
numpy>=1.23.5
scipy>=1.10.0
scikit-learn>=1.2.0
numba>=0.56.4
Cython>=0.29.30

# ── Audio processing ─────────────────────────────────────────────────────────
librosa>=0.9.1
pyworld>=0.3.2
praat-parselmouth>=0.4.2
soundfile>=0.12.0
sounddevice>=0.4.6
ffmpeg-python>=0.2.0
stftpitchshift>=1.5.1

# ── Vector indexing ──────────────────────────────────────────────────────────
faiss-cpu>=1.7.3

# ── ONNX inference (required for MDXNet / UVR5 ONNX models) ─────────────────
onnxruntime-gpu>=1.15.0

# ── GUI ──────────────────────────────────────────────────────────────────────
PyQt6>=6.5.0

# ── Visualisation & monitoring (used by training pipeline) ───────────────────
matplotlib>=3.6.2
tensorboard>=2.13.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.65.0
PyYAML>=6.0
requests>=2.28.0
protobuf>=4.22.0
